# AGENTS.md

## 文档维护规则（重要）

本仓库 `日常开发/` 目录下有 4 份持续维护的文档：

| 文档 | 内容 | 何时更新 |
|---|---|---|
| `日常开发/项目结构.md` | 目录结构、模块职责、关键数据路径 | 新增/移动/删除文件或模块时 |
| `日常开发/配置操作.md` | 仓库/代理/凭据/Rust 环境、提交推送、同步上游 | 环境、git 配置、远端变化时 |
| `日常开发/安装调试.md` | 依赖安装、dev/build/安装/使用注意 | 依赖、构建方式、产物路径变化时 |
| `日常开发/改动记录.md` | 每次改动的提交与说明 | **每次代码改动后必须追加记录** |

规则：

1. **每次改动代码或梳理项目后，必须同步更新上述对应文档**；代码改动必更新 `改动记录.md`
2. `日常开发/` 目录**只保留在本地，禁止提交、禁止推送远程**（已在 `.git/info/exclude` 排除）
3. 代码与文档冲突时以代码为准，并立即修正文档

## Git 规范

- commit message 必须以 `feat:` 或 `fix:` 开头，不超过 20 字
- 推送到 GitHub 需走本机代理（已配置 `http.https://github.com.proxy`）

## 技术要点

- Tauri 2 + React 19 + rusqlite；Tauri commands 在 `src-tauri/src/lib.rs`
- macOS 上「继续」按钮必须用 osascript 打开 Terminal.app（TUI 需要 tty），禁止改回后台 spawn
- 构建发版命令见 `日常开发/安装调试.md`
